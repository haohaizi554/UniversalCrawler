# 更新日志

## 2026-07-08 文档内容适配

- 将活文档中的 Web 启动命令更新为当前 `entry.web_entry` 入口，保留历史复盘中的问题背景语境。
- 更新接口指南的 WebSocket 说明，补齐 `ws_router.py`、`ws_dispatcher.py` 和 `ws_runtime.py` 的职责分工。
- 更新架构、开发和文档中心说明，使其与 `3.6.17` 阶段的 GUI/WebUI 状态一致性、前端刷新、并发控制、国际化和测试维护重点对齐。

## 2026-07-02 文档整理

- 统一 `docs/` 目录结构：指南、CLI/SDK、修复记录、工程审查、验收报告、事故复盘、ADR 和 Prompt 分区。
- 统一文件名规范：普通文档使用小写英文 slug，报告和审查使用日期前缀。
- 删除重复的打包入口说明，保留 `docs/guides/packaging.md`。
- 将明显过时或乱码的重构记录改写为当前工程可用的中文说明。
- 合并信号基线和信号优化验收为一份阶段报告，并新增前端刷新与并发控制工程实践。
- 将已被总览吸收的单点修复记录合并到 `docs/fixes/` 同级主题文档，不再维护二级归档目录。
- 将 GUI/WebUI 设置、下拉框和国际化契约移动到工程实践目录，作为当前准则维护。

> 历史功能变更请结合 Git 提交记录和阶段验收报告查看。
